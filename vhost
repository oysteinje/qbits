server {
       listen 80;
       listen [::]:80;

       server_name main.azure.qbits.no;

       root /var/www/main;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }

       access_log  /var/log/nginx/domain1.access.log;
}