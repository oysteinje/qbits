server {
       listen 81;
       listen [::]:81;

       server_name backend-ystrte.norwayeast.cloudapp.azure.com;

       root /var/www/main;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}