server {
       listen 81;
       listen [::]:81;

       server_name ssh.azure.qbits.no;

       root /var/www/main;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}